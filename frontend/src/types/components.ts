export enum InputType {
  text = "text",
  number = "number",
  url = "url",
  password = "password",
}

export enum ButtonType {
  submit = "submit",
  button = "button",
}

export enum IconName {
  trash = "trash",
  edit = "edit",
  send = "paper-plane",
  close = "times",
}

export enum Size {
  sm = "small",
  md = "medium",
};


export enum Color {
  gray = "--gray",
  primary = "--primary",
  error = "--error",
  white = "--white",
  transparent = "--transparent",
}
